# Copy to .env and adjust for your local setup.
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_USERNAME=admin
MQTT_PASSWORD=change-me
SERVER_PORT=3000
# 16-byte key for AES-128-GCM (dev only — generate your own, e.g. `openssl rand -hex 8`)
ENCRYPTION_KEY=change-me-16bytes
HEARTBEAT_INTERVAL=8000
